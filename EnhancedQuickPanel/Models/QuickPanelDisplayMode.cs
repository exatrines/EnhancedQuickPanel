namespace EnhancedQuickPanel.Models;

/// <summary>Whether the native quick panel, the plugin overlay, or neither is shown for /quickpanel.</summary>
public enum QuickPanelDisplayMode
{
    NativeOnly = 0,
    PluginOnly = 1,
}

